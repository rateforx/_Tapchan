// Generated by CoffeeScript 1.9.1
(function() {
  var check;

  module.exports = check = {
    absoluteFilePath: function(filePath) {
      if (!filePath) {
        return false;
      }
      return /^(\/|\w:)/.test(filePath);
    },
    fileExtension: function(filePath, ext) {
      var regex;
      if (!filePath || typeof filePath !== 'string' || !ext) {
        return false;
      }
      regex = ext != null ? (ext = ext.replace(/^\./, ''), new RegExp("." + ext + "$", 'i')) : /\.([^\.]+)$/;
      return filePath != null ? filePath.match(regex) : void 0;
    },
    url: function(str) {
      return str.match(/^((ht|f)tps?\:\/\/|www\.)[a-zA-Z0-9\-\.]+\.[a-zA-Z]{2,3}(\/\S*)?/);
    },
    number: function(n) {
      return !isNaN(parseFloat(n)) && isFinite(n);
    }
  };

}).call(this);
