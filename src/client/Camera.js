/**
 * Created by Snippy on 2017-05-17.
 */

class Camera {

    constructor() {

        this.position = {
            x, y,
            set: (x, y) => {
                this.x = x;
                this.y = y;
            }
        };
        this.position.set(0, 0);

        this.scale = 1;
        this.
    }

}